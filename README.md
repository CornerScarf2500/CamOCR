# CamOCR
# Local Hybrid OCR - Snapdragon NPU Powered

Beautiful web UI for mixed-language + handwriting OCR with automatic data collection.

## Quick Start (Local)
```bash
git clone https://github.com/yourusername/Local-Hybrid-OCR.git
cd Local-Hybrid-OCR
pip install -r requirements.txt
python app.py
